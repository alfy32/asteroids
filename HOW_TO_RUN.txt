to start server run the following commands:

npm install
npm start

if you run the server using node app.js it will not work. It has to be done from the root directory and run as node server/app.js. So just use the npm start command.

The server can be found at http://localhost:3000